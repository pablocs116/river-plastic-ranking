# Normalize all text files to LF in the repo; check out platform-native.
# Stops Windows/WSL CRLF churn from showing every file as "modified".
* text=auto eol=lf

# Binary / data files: never touch
*.tif binary
*.zip binary
*.nc binary
*.csv text eol=lf
*.ipynb text eol=lf
